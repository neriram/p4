import Vue from 'vue';
import Vuetify from 'vuetify/lib';

Vue.use(Vuetify);

export default new Vuetify({
    theme: {
        primary: '#ffeb3b',
        secondary: '#00bcd4',
        accent: '#ff9800',
        error: '#2196f3',
        warning: '#ff5722',
        info: '#8bc34a',
        success: '#4caf50'
        }
});
